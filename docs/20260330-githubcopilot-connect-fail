好的！我来为你整理一份**完整的操作指南**，涵盖从零开始配置的所有步骤。

---

# GitHub Copilot 远端服务器代理配置完整指南

## 📋 目录
1. [问题背景](#问题背景)
2. [解决方案概述](#解决方案概述)
3. [前置条件](#前置条件)
4. [第一步：配置本地 Clash Verge](#第一步配置本地-clash-verge)
5. [第二步：配置 SSH 端口转发](#第二步配置-ssh-端口转发)
6. [第三步：连接远端服务器并测试](#第三步连接远端服务器并测试)
7. [第四步：配置 VS Code 远程设置](#第四步配置-vs-code-远程设置)
8. [第五步：验证 Copilot 正常工作](#第五步验证-copilot-正常工作)
9. [常见问题排查](#常见问题排查)
10. [安全说明](#安全说明)
11. [配置总结](#配置总结)

---

## 问题背景

### 症状
- 远端 Linux 服务器无法访问 `api.individual.githubcopilot.com`
- Copilot 报错：`fetch failed` 或 `connect ECONNREFUSED`
- 本地 Windows 可以正常访问 Copilot API

### 根本原因
- 远端服务器和本地 Windows 在不同子网（如 `192.168.8.x` 和 `192.168.5.x`）
- 远端服务器网络策略限制了 Copilot API 的访问
- 本地网络可以正常访问

### 解决方案
通过 SSH 反向端口转发，让远端服务器的 Copilot 流量走本地 Windows 的 Clash 代理。

---

## 解决方案概述

```
┌─────────────────────────────────────────────────────────────────┐
│  本地 Windows (192.168.5.145)                                   │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ Clash Verge (端口 7897)                                  │    │
│  │ 允许局域网连接                                            │    │
│  └─────────────────────────────────────────────────────────┘    │
│                              ▲                                  │
│                              │ SSH 反向隧道                      │
│                              │ RemoteForward 7890 → localhost:7897│
│                              │                                  │
└──────────────────────────────┼──────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│  远端 Linux 服务器 (192.168.8.14)                               │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ Copilot 扩展 (运行在 workspace 端)                        │    │
│  │ 代理配置: http://127.0.0.1:7890                          │    │
│  │ 流量通过 SSH 隧道 → 本地 Clash → Copilot API              │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
```

---

## 前置条件

| 项目 | 要求 |
|------|------|
| 本地 Windows | 已安装 Clash Verge，正常运行 |
| 本地 Windows | 能访问 `api.individual.githubcopilot.com` |
| 远端服务器 | SSH 可连接（IP、用户名、密码/密钥） |
| VS Code | 已安装 Remote-SSH 插件 |

---

## 第一步：配置本地 Clash Verge

### 1.1 打开 Clash Verge
确保 Clash Verge 正在运行（系统托盘图标为绿色）。

### 1.2 确认代理端口
- 点击左侧菜单 **设置** 或 **General**
- 找到 **混合端口 (Mixed Port)**
- 确认端口号为 `7897`（如果不是，记下你的端口号）

### 1.3 开启局域网连接
- 找到 **允许局域网连接 (Allow LAN)**
- 确保该选项已开启 ✅

### 1.4 获取本地 Windows IP
打开 **PowerShell**，执行：
```powershell
ipconfig
```
找到正在使用的网卡（通常是 `以太网适配器 以太网 3`），记录 **IPv4 地址**：
```
IPv4 地址 . . . . . . . . . . . . : 192.168.5.145
```
记下这个 IP，后面备用。

---

## 第二步：配置 SSH 端口转发

有两种方式，推荐方式一（一次配置，永久生效）。

### 方式一：通过 SSH 配置文件（推荐）

#### 2.1 打开 SSH 配置文件
1. 在 VS Code 中按 `Ctrl + Shift + P`
2. 输入 `Remote-SSH: Open SSH Configuration File`
3. 选择你的配置文件（通常是 `C:\Users\Administrator\.ssh\config`）

#### 2.2 添加端口转发配置
在文件末尾添加以下内容：

```
Host 192.168.8.14
    HostName 192.168.8.14
    User 你的用户名
    RemoteForward 7890 localhost:7897
```

**示例**（假设用户名为 `root`）：
```
Host 192.168.8.14
  HostName 192.168.8.14
  User dingkp@mail.ustc.edu.cn
  RemoteForward 7890 localhost:7897
  GatewayPorts no
```

#### 2.3 保存文件
按 `Ctrl + S` 保存。

---

### 方式二：手动建立隧道（临时测试用）

如果不想修改配置文件，每次连接后在本地 Windows 的 PowerShell 中执行：

```bash
ssh -R 7890:127.0.0.1:7897 -N -f 你的用户名@192.168.8.14
```

输入密码后，隧道在后台运行。**注意**：这个终端窗口不能关闭。

---

## 第三步：连接远端服务器并测试

### 3.1 连接到远端服务器
1. 在 VS Code 中按 `Ctrl + Shift + P`
2. 输入 `Remote-SSH: Connect to Host...`
3. 选择 `192.168.8.14`
4. 输入密码（如果配置了密钥则自动连接）

连接成功后，左下角会显示 `SSH: 192.168.8.14`。

### 3.2 测试代理连通性
在 VS Code 的终端中（已自动连接到远端服务器），执行：

```bash
curl -v --proxy http://127.0.0.1:7890 -k https://api.individual.githubcopilot.com/_ping
```

**预期结果**：
```
< HTTP/2 200
< date: ...
```
看到 `HTTP/2 200` 表示代理配置成功！

---

## 第四步：配置 VS Code 远程设置

### 4.1 打开远程设置
1. 在 VS Code 中（已连接远程服务器），按 `Ctrl + Shift + P`
2. 输入 `Preferences: Open Remote Settings (JSON)`

### 4.2 添加配置
在打开的 `settings.json` 中添加：

```json
{
    "http.proxy": "http://127.0.0.1:7890",
    "http.proxyStrictSSL": false,
    "remote.extensionKind": {
        "github.copilot": ["workspace"],
        "github.copilot-chat": ["workspace"]
    }
}

```

### 4.3 保存文件
按 `Ctrl + S` 保存。

---

## 第五步：验证 Copilot 正常工作

### 5.1 重启 VS Code
1. 完全退出 VS Code（关闭所有窗口）
2. 重新打开 VS Code
3. 连接到远端服务器 `192.168.8.14`

### 5.2 测试 Copilot
在 VS Code 中打开任意代码文件，输入注释，例如：

```python
# 写一个函数计算斐波那契数列
```

如果出现灰色的代码补全提示，说明 Copilot 正常工作！

---

## 常见问题排查

### Q1: curl 测试返回 `Connection refused`

**检查 SSH 隧道是否建立**：
```bash
netstat -tlnp | grep 7890
```

有输出说明隧道正常，无输出说明隧道未建立。

**解决方法**：
- 检查 SSH 配置文件中的 `RemoteForward` 是否正确
- 或手动重新建立隧道：`ssh -R 7890:127.0.0.1:7897 ...`

---

### Q2: curl 测试返回 TLS 错误

**解决方法**：使用 `-k` 参数跳过证书验证：
```bash
curl -v --proxy http://127.0.0.1:7890 -k https://api.individual.githubcopilot.com/_ping
```

VS Code 的 `http.proxyStrictSSL: false` 已做同样处理。

---

### Q3: Copilot 仍报网络错误

**检查 VS Code 远程设置是否生效**：
```bash
echo $HTTP_PROXY
echo $HTTPS_PROXY
```

如果为空，手动设置环境变量：
```bash
export HTTP_PROXY=http://127.0.0.1:7890
export HTTPS_PROXY=http://127.0.0.1:7890
```

---

### Q4: 电脑关机后其他人还能访问吗

**可以**。你的隧道只在你电脑开机且 SSH 连接存在时才有效。其他人有自己的 SSH 连接，完全不受影响。

---

## 安全说明

### 端口 7890 是否暴露？

**不会暴露**。SSH 反向隧道默认绑定在 `127.0.0.1`，这意味着：

| 谁能访问 | 能否访问 |
|---------|---------|
| 远端服务器本机 | ✅ 可以 |
| 同一内网的其他机器 | ❌ 不可以 |
| 公网 | ❌ 不可以 |

### 验证端口绑定
在远端服务器执行：
```bash
netstat -tlnp | grep 7890
```
如果输出 `127.0.0.1:7890`，说明安全；如果是 `0.0.0.0:7890`，说明暴露，需要检查 SSH 配置。

---

## 配置总结

| 组件 | 配置值 |
|------|--------|
| 本地 Clash 端口 | `7897` |
| 本地 Windows IP | `192.168.5.145`（根据实际修改） |
| SSH 转发规则 | `RemoteForward 7890 localhost:7897` |
| 远程代理地址 | `http://127.0.0.1:7890` |
| Copilot 运行位置 | `workspace`（远端） |

---

## 检查清单

- [ ] Clash Verge 运行中，端口 `7897`，**允许局域网连接**已开启
- [ ] SSH 配置文件已添加 `RemoteForward 7890 localhost:7897`
- [ ] 能成功连接到远端服务器
- [ ] `curl --proxy http://127.0.0.1:7890 -k https://api.individual.githubcopilot.com/_ping` 返回 `200`
- [ ] VS Code 远程设置已配置 `http.proxy`
- [ ] 重启后 Copilot 正常工作

---

完成所有步骤后，GitHub Copilot 应该能在远端服务器上稳定工作了！如有任何问题，欢迎随时提问。
